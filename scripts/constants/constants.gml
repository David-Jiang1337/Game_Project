enum weaponMode {
	auto,
	semi,
}

enum enemyMode {
	patrol,
	alerted,
}

enum cellSize {
	height = 32,
	width = 32,
}

enum bitmask {
	north = 1,
	west = 2,
	east = 4,
	south = 8,
}